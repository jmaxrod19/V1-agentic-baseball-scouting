pandas>=2.0
pybaseball>=2.2.7
matplotlib>=3.7        # static report charts (embedded as PNG)
scipy>=1.10            # gaussian_kde for pitch-location heatmaps
duckdb>=1.0            # in-process SQL over pandas frames (swing-geometry card)
fastapi>=0.110         # web app (webapp.py)
uvicorn[standard]>=0.29  # ASGI server that runs the FastAPI app
python-multipart>=0.0.9  # lets FastAPI parse the HTML form POST
anthropic>=0.40          # Claude API — the agentic scouting narrative
